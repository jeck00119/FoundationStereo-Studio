# FoundationStereo Studio — Jetson Orin (aarch64, JetPack 6.x, Python 3.10).
# torch/torchvision are NOT in this file: standard PyPI wheels are x86-only.
# setup_jetson.sh installs them from the Jetson wheel index first; this file
# covers everything else.
#
# Differences vs the Windows requirements.txt:
#  - no flash-attn (no Jetson wheel; FoundationStereo falls back to standard
#    attention per its FAQ)
#  - no xformers (x86 wheel; not required by the studio pipeline)
#  - triton is not listed here: PyPI has no Jetson build — setup_jetson.sh
#    installs it from the Jetson wheel index next to torch (it powers
#    Fast-FoundationStereo's compiled cost-volume path; without it the eager
#    fallback needs ~4x the memory)
#  - versions are floors, not pins — aarch64 wheel availability differs from
#    x86, so let pip resolve within these bounds.

# GUI
PySide6>=6.6            # Essentials + Addons (QtWebEngine for the 3D view)
pyqtgraph>=0.13
matplotlib>=3.8

# vision / math
numpy
opencv-contrib-python>=4.9
imageio>=2.34
scipy>=1.11
open3d>=0.18            # cloud denoise + PLY I/O (aarch64 wheels exist; if pip
                        # finds none for your Python, the app still runs — the
                        # denoise step skips itself and says so)

# model stack
omegaconf>=2.3
timm>=1.0
einops>=0.7
huggingface_hub

# dev
pytest
