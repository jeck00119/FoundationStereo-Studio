pretrained_models/*
output/*
*.pyc
weight/
__pycache__
DATA/*

# --- local environment & outputs (studio app) ---
.venv/
test_outputs/
.cache/
.pytest_cache/
studio/_spike_web/
*.log

# data/ stays local — EXCEPT calibration .json files, which are small,
# device-independent, and needed on every machine (the Orin gets them via git)
data/*
!data/calib
data/calib/*
!data/calib/*.json